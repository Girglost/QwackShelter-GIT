import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-bas-footer',
  imports: [RouterLink],
  templateUrl: './bas-footer.html',
  styleUrl: './bas-footer.css',
})
export class BasFooter {}
