import { AnimalImageUtils } from './animal-image-utilsutils';

describe('AnimalImageUtils', () => {
  it('should create an instance', () => {
    expect(new AnimalImageUtils()).toBeTruthy();
  });
});
